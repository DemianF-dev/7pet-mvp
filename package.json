{
  "name": "7pet-mvp-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && prisma generate",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build": "npm run build:frontend"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}